# MarketVision - 完全実装仕様書

**📅 最終更新**: 2025年12月15日  
**ステータス**: ✅ Production Ready  
**公開URL**: https://j1921604.github.io/MarketVision/  
**GitHubリポジトリ**: https://github.com/J1921604/MarketVision  
**バージョン**: 1.0.0  
**リリース日**: 2025年12月15日

---

## 🎯 このドキュメントについて

この仕様書は、**MarketVision（株価テクニカル分析ダッシュボード）の完全な実装仕様書**です。
GitHub ActionsによるCI/CD自動化、Stooq API連携、テクニカル指標計算を含みます。

---

## システム概要

### プロジェクト概要

MarketVision は、東京電力HD（9501.T）と中部電力（9502.T）の株価を可視化し、テクニカル分析を支援する Web アプリケーションです。

### 主要機能

1. **株価チャート表示**: ローソク足チャート（日足）で過去10年間の株価推移を可視化
2. **テクニカル指標**: SMA（5/25/50/75日）、RSI（14日）、MACD（12/26/9日）、ボリンジャーバンド（20日、2σ）
3. **銘柄比較**: 2銘柄の価格推移を同一チャートで比較
4. **期間フィルタ**: 1M/3M/6M/1Y/3Y/5Y/カスタム

### アーキテクチャ

電力会社株価分析システムは、以下のコンポーネントで構成されています:

1. **データ取得層**: Stooq API から株価データを取得（pandas_datareader）
2. **指標計算層**: Python でテクニカル指標（SMA/RSI/MACD/BB）を計算
3. **可視化層**: React + TypeScript + Recharts でチャート表示
4. **デプロイ層**: GitHub Actions で自動ビルド・デプロイ

### ワークフロー

```
毎日 JST 07:00 (GitHub Actions Cron) または mainブランチへのPush
↓
Stooq API → 株価データ取得 (fetch_price_data.py)
↓
テクニカル指標計算 (build_indicators.py)
↓
データ検証 (validate_data.py)
↓
フロントエンドビルド (npm run build)
↓
GitHub Pages自動デプロイ
```

---

## データモデル

### 株価データ (PriceData)

```typescript
interface PriceData {
  date: string;     // YYYY-MM-DD形式
  open: number;     // 始値
  high: number;     // 高値
  low: number;      // 安値
  close: number;    // 終値
  volume: number;   // 出来高
}
```

### 移動平均線データ (SMAData)

```typescript
interface SMAData {
  date: string;
  sma_5: number;    // 5日移動平均
  sma_25: number;   // 25日移動平均
  sma_50: number;   // 50日移動平均
  sma_75: number;   // 75日移動平均
}
```

### RSIデータ (RSIData)

```typescript
interface RSIData {
  date: string;
  rsi: number;      // RSI（0-100）
}
```

### MACDデータ (MACDData)

```typescript
interface MACDData {
  date: string;
  macd: number;         // MACD線
  signal: number;       // シグナル線
  histogram: number;    // ヒストグラム
}
```

### ボリンジャーバンドデータ (BollingerBandData)

```typescript
interface BollingerBandData {
  date: string;
  upper: number;    // 上限（+2σ）
  middle: number;   // 中央（SMA20）
  lower: number;    // 下限（-2σ）
}
```

### データソース

| データ種別       | 期間       | ソース                        | ファイル形式                   |
| ---------------- | ---------- | ----------------------------- | ------------------------------ |
| 株価（9501.T）   | 過去10年   | Stooq API                     | `public/data/price/9501.T.csv` |
| 株価（9502.T）   | 過去10年   | Stooq API                     | `public/data/price/9502.T.csv` |
| SMA指標          | 過去10年   | Python計算（pandas）           | `public/data/indicators/sma_9501.T.json` |
| RSI指標          | 過去10年   | Python計算（pandas）           | `public/data/indicators/rsi_9501.T.json` |
| MACD指標         | 過去10年   | Python計算（pandas）           | `public/data/indicators/macd_9501.T.json` |
| BB指標           | 過去10年   | Python計算（pandas）           | `public/data/indicators/bb_9501.T.json` |

---

## テクニカル指標計算式

### 1. 移動平均線（SMA: Simple Moving Average）

**計算式**:
```python
SMA_n = Σ(Close[i-n+1 to i]) / n  
**予測時間**: < 1秒  
**期待精度**: R² > 0.90

### 2. Keras (ディープラーニング)

**アーキテクチャ**:
```python
Sequential([
    Dense(128, activation='relu', input_dim=4),
    Dense(64, activation='relu'),
    Dense(32, activation='relu'),
    Dense(1)
])
```

**ハイパーパラメータ**:
```python
{
    "epochs": 50,
    "batch_size": 32,
    "validation_split": 0.1,
    "optimizer": "adam"
}
```

**学習時間**: 約30秒  
**予測時間**: < 1秒  
**期待精度**: R² > 0.88

### 3. RandomForest

**種別**: ランダムフォレスト

**ハイパーパラメータ**:
```python
{
    "n_estimators": 100,
    "max_depth": None,
    "random_state": 42
}
```

**学習時間**: 約15秒  
**予測時間**: < 1秒  
**期待精度**: R² > 0.89

### 4. PyCaret (AutoML)

**種別**: AutoML自動モデル選択

**設定**:
```python
{
    "session_id": 42,
    "verbose": False,
    "n_jobs": -1  # 全コア使用
}
```

**学習時間**: 約60秒  
**予測時間**: < 1秒  
**期待精度**: R² > 0.90

---

## 精度指標

### RMSE (Root Mean Squared Error)

$$
\text{RMSE} = \sqrt{\frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2}
$$

**単位**: kW  
**目標値**: < 500 kW

### R² (決定係数)

$$
R^2 = 1 - \frac{\sum_{i=1}^{n} (y_i - \hat{y}_i)^2}{\sum_{i=1}^{n} (y_i - \bar{y})^2}
$$

**範囲**: 0 ~ 1  
**目標値**: > 0.80  
**アラート閾値**: < 0.80 で自動Issue作成

### MAE (Mean Absolute Error)

$$
\text{MAE} = \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i|
$$

**単位**: kW  
**目標値**: < 400 kW

---

## GitHub Actions ワークフロー

### トリガー

1. **Cron**: 毎日 UTC 22:00 (JST 07:00)
2. **Push**: mainブランチへのプッシュ
3. **手動**: workflow_dispatch

### ジョブフロー

```yaml
jobs:
  forecast:
    steps:
      - Checkout
      - Setup Python 3.11
```
例: SMA_5 = (Close[0] + Close[1] + ... + Close[4]) / 5

**パラメータ**:
- SMA_5: 5日移動平均
- SMA_25: 25日移動平均
- SMA_50: 50日移動平均
- SMA_75: 75日移動平均

**用途**: トレンド分析、サポート/レジスタンスライン

---

### 2. RSI（Relative Strength Index）

**計算式**:
```python
RS = 平均上昇幅 / 平均下落幅
RSI = 100 - (100 / (1 + RS))
```

**パラメータ**:
- 期間: 14日

**範囲**: 0～100
- 70以上: 買われすぎ
- 30以下: 売られすぎ

**用途**: 買われすぎ・売られすぎの判断

---

### 3. MACD（Moving Average Convergence Divergence）

**計算式**:
```python
MACD = EMA_12 - EMA_26
Signal = EMA_9(MACD)
Histogram = MACD - Signal
```

**パラメータ**:
- 短期EMA: 12日
- 長期EMA: 26日
- シグナル: 9日

**用途**: トレンド転換点の検出

---

### 4. ボリンジャーバンド（Bollinger Bands）

**計算式**:
```python
Middle = SMA_20
Upper = Middle + (2 × σ)
Lower = Middle - (2 × σ)
```

**パラメータ**:
- 期間: 20日
- 標準偏差: 2σ

**用途**: ボラティリティ分析、価格レンジ予測

---

## フロントエンド仕様

### 技術スタック

| 技術           | バージョン | 用途                  |
| -------------- | ---------- | --------------------- |
| React          | 18.2       | UIフレームワーク      |
| TypeScript     | 5.3        | 型安全性              |
| Vite           | 5.0        | ビルドツール          |
| Recharts       | 2.10       | チャート描画          |
| Tailwind CSS   | 3.4        | スタイリング          |

### コンポーネント構成

```
src/
├── App.tsx              # メインアプリケーション
├── components/
│   ├── ChartCanvas.tsx  # ローソク足チャート
│   ├── TechnicalPanel.tsx  # テクニカル指標パネル
│   ├── RSIChart.tsx     # RSIチャート
│   └── MACDChart.tsx    # MACDチャート
├── hooks/
│   └── useMarketData.ts # データフック
└── types/
    └── index.ts         # 型定義
```

### デザインシステム

**テーマ**: Cyberpunk Neumorphism

**カラーパレット**:
```css
--bg: #0A0F0F          /* ダークベース */
--fg: #E6F5F1          /* テキスト */
--neon-green: #00FF84  /* ネオングリーン */
--cyan: #00D4FF        /* サイバーブルー */
--magenta: #FF2ECC     /* マゼンタ */
--surface: #0D1414     /* サーフェス */
--border: #1A2F2F      /* ボーダー */
```

---

## GitHub Actions ワークフロー

### トリガー

1. **Cron**: 毎日 UTC 22:00 (JST 07:00)
2. **Push**: mainブランチへのプッシュ
3. **手動**: workflow_dispatch

### ジョブフロー

```yaml
jobs:
  build:
    steps:
      - Checkout
      - Setup Python 3.11
      - Install Python dependencies
      - Fetch stock price data (Stooq API)
      - Build technical indicators
      - Setup Node.js 20
      - Install Node dependencies
      - Build frontend (Vite)
      - Upload artifact
  
  deploy:
    needs: build
    steps:
      - Deploy to GitHub Pages
```

### 成果物

- `dist/index.html`: メインHTML
- `dist/assets/*.js`: バンドルJS
- `dist/assets/*.css`: バンドルCSS
- `public/data/`: 株価データとテクニカル指標

---

## Stooq API仕様

### データ取得

```python
import pandas_datareader as pdr

# 株価データ取得
df = pdr.get_data_stooq('9501.T', start='2015-01-01', end='2025-12-31')
```

### レスポンス形式

```csv
Date,Open,High,Low,Close,Volume
2025-12-01,500.0,510.0,495.0,505.0,1000000
```

### 銘柄コード

- **東京電力HD**: 9501.T
- **中部電力**: 9502.T

---

## デプロイ仕様

### GitHub Pages設定

| 項目   | 値                                          |
| ------ | ------------------------------------------- |
| Source | GitHub Actions                              |
| URL    | https://j1921604.github.io/MarketVision/ |

### ディレクトリ構成

```
dist/  (GitHub Pages デプロイディレクトリ)
├── index.html
├── assets/
│   ├── index-[hash].js
│   └── index-[hash].css
└── data/
    ├── price/
    │   ├── 9501.T.csv
    │   └── 9502.T.csv
    └── indicators/
        ├── sma_9501.T.json
        ├── rsi_9501.T.json
        ├── macd_9501.T.json
        └── bb_9501.T.json
```

---

## プロジェクト構造

```
MarketVision/
├── .github/
│   └── workflows/
│       └── deploy-pages.yml
├── src/
│   ├── App.tsx
│   ├── main.tsx
│   ├── index.css
│   ├── components/
│   │   ├── ChartCanvas.tsx
│   │   ├── TechnicalPanel.tsx
│   │   ├── RSIChart.tsx
│   │   └── MACDChart.tsx
│   ├── hooks/
│   │   └── useMarketData.ts
│   └── types/
│       └── index.ts
├── scripts/
│   ├── fetch_price_data.py
│   ├── build_indicators.py
│   ├── validate_data.py
│   └── requirements.txt
├── public/
│   └── data/
│       ├── price/
│       └── indicators/
├── docs/
│   ├── 完全仕様書.md  (本ファイル)
│   └── DEPLOY_GUIDE.md
├── tests/
│   └── e2e/
│       └── marketvision.spec.ts
├── package.json
├── vite.config.ts
├── tsconfig.json
└── README.md
```

---

## トラブルシューティング

### 問題1: GitHub Actions失敗

**症状**: ワークフローが途中で失敗

**確認**:
```powershell
# ローカルで再現
py scripts/fetch_price_data.py --symbols "9501.T,9502.T" --output public/data/price
py scripts/build_indicators.py --symbols "9501.T,9502.T" --input public/data/price --output public/data/indicators
```

**解決**:
- Stooq API タイムアウト → リトライ実装済み
- 依存関係不足 → `requirements.txt` 確認

### 問題2: GitHub Pages 404

**症状**: デプロイ成功だがページが表示されない

**確認**:
- Settings → Pages → Source: **GitHub Actions**
- `dist/` ディレクトリに `index.html` が存在するか

**解決**:
```powershell
# ワークフローを手動再実行
# https://github.com/J1921604/MarketVision/actions
# → "Run workflow"
```

### 問題3: データが表示されない

**症状**: チャートにデータが表示されない

**確認**:
```powershell
# データファイル確認
Get-ChildItem -Recurse public/data/
```

**解決**:
```powershell
# ローカルでデータ取得
py scripts/fetch_price_data.py --symbols "9501.T,9502.T" --output public/data/price
py scripts/build_indicators.py --symbols "9501.T,9502.T" --input public/data/price --output public/data/indicators
```

---

## 参考リンク

- **Live Demo**: https://j1921604.github.io/MarketVision/
- **GitHubリポジトリ**: https://github.com/J1921604/MarketVision
- **GitHub Actions**: https://github.com/J1921604/MarketVision/actions
- **デプロイガイド**: [DEPLOY_GUIDE.md](https://github.com/J1921604/MarketVision/blob/main/docs/DEPLOY_GUIDE.md)
- **Stooq**: https://stooq.com/
- **Recharts**: https://recharts.org/
- **React**: https://react.dev/
- **TypeScript**: https://www.typescriptlang.org/
- **Vite**: https://vitejs.dev/

---

**Last Updated**: 2025-12-15  
**Version**: 1.0.0

